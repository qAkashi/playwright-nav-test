{
  "name": "playwright-nav-test",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "npx playwright test --config=playwright.config.js",
    "test:headed": "npx playwright test --headed --config=playwright.config.js"
  },
  "devDependencies": {
    "playwright": "^1.51.0",
    "@playwright/test": "^1.51.0",
    "http-server": "^14.1.1"
  }
}
